<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Message Edit Form</title>
</head>
<body>
    <h3>Edit your message : <%=chat.id%></h3>
    <p>This chat was sent from <b><%=chat.from%></b> to <b><%=chat.to%></b></p>
    <form method="post" action="/chats/<%=chat._id%>?_method=PUT">
        <textarea name="message" rows="5" cols="50"><%=chat.msg%></textarea>
        <br>
        <button>Edit</button>

    </form>
</body>
</html>